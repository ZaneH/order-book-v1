* Order Book V1
See [[./docs/00_COURSEWORK.org][00_COURSEWORK.org]] for the project specs. Based on [[https://github.com/alissawu/miniex][alissawu/miniex]].

This project uses the [[https://google.github.io/styleguide/cppguide.html][Google C++ Style Guide]].

** Build and Run

#+begin_src sh
  $ mkdir build
  $ cmake -S . -B build -DCMAKE_BUILD_TYPE=Release # or Debug
  $ cmake --build build
  $ cd build && make
  $ ctest # Run tests (add --verbose for more detail)
  $ ./clob_cli # Start orderbook
#+end_src
